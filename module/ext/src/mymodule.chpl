module mymodule {
    export
    proc add_rr(x: real, y: real) {
        return x + y;
    }

    export
    proc subtract_rr(x: real, y: real) {
        return x + y;
    }

    export
    proc hello_mymodule() {
        writeln("Hello from mymodule");
    }
}
